import React from 'react';

const App = () => (
  <div id="main">
    <NavBar />
  </div>
);

const NavBar = () => (
  <nav className="navbar">
    <div className="container-fluid">
      HELLO
    </div>
  </nav>
);

export default App;
